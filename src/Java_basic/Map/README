MAP
 - 키와 값으로 구성된 엔트리 객체를 저장
 - 키와 값은 모두 객체이다. (키는 중복될 수 없지만 값은 중복 가능하다. 동일한 키로 값을 저장하면 기존값은 삭제되고  새로운값 대체
 - put(K key,V value)으로 값 넣고 // v get(Object key)로 조회 // (V remove(Object key)로 삭제  엔트리 자체가 삭제된다.)



Hashtable
    -Hashtable 은 hashMap 과 동일한 내부 구조.
      차이점은 hashtable 은 동기화된 (synchronized) 메소드로 구성되어 있어 멀티스레다가 동시에 HastTable 의 메소드를 실행할 수 없다.
       따라서 멀티 스레드 환경에서도 안전하게 객체를 추가 ,삭제 가능함


    -Hashtable 특징은 키와 값은 String 타입으로 제한되는 컬렉션임, 주로 확장자가 .properties 인 프로퍼티를 읽을때 사용.
     URL getResource(String)  /  리소스 파일의 URL 리턴해줌
